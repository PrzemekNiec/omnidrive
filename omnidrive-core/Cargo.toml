[package]
name = "omnidrive-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
rmp-serde = "1"
zerocopy = { version = "0.7", features = ["derive"] }
